<%
# TEMPORARY FIX for s390x: Comment out network line to avoid dracut parse-kickstart issues
# Network will be configured via kernel parameters (ip=dhcp rd.neednet=1) in TDL
# TODO: Make this conditional on @arch once ARCH environment variable is properly set
%>
<% if @target == "ec2" || @target == "azure" %>
# network --bootproto=dhcp --device=link --activate --onboot=on --noipv6
<% elsif @target == "gce" %>
# network --bootproto=dhcp --device=link --activate --onboot=on --noipv6 --mtu=1460
<% else %>
# network --bootproto=dhcp --device=link --activate --onboot=on
<% end %>
